# Production Docker Compose Configuration
# Use: docker-compose -f docker-compose.production.yml up -d

version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    env_file:
      - .env.production
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres-core:/var/lib/postgresql/data
    networks:
      - freebase-core
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 500M
        reservations:
          cpus: '0.5'
          memory: 256M
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER}']
      interval: 10s
      timeout: 5s
      retries: 5

  traefik:
    image: traefik:v3.0
    env_file:
      - .env.production
    command:
      - '--api.dashboard=false'
      - '--providers.docker=true'
      - '--providers.docker.exposedbydefault=false'
      - '--entrypoints.web.address=:80'
      - '--entrypoints.websecure.address=:443'
      - '--certificatesResolvers.letsencrypt.acme.httpChallenge=true'
      - '--certificatesResolvers.letsencrypt.acme.httpChallenge.entryPoint=web'
      - '--certificatesResolvers.letsencrypt.acme.email=${ACME_EMAIL}'
      - '--certificatesResolvers.letsencrypt.acme.storage=/letsencrypt/acme.json'
      - '--log.level=WARN'
      - '--accessLog=true'
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./docker/traefik/traefik.yml:/traefik.yml:ro
      - ./docker/traefik/dynamic:/etc/traefik/dynamic:ro
      - traefik-certs:/letsencrypt
    networks:
      - freebase-core
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 500M
        reservations:
          cpus: '0.25'
          memory: 128M

  backend:
    image: ghcr.io/marion909/freebase/backend:latest
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      LOG_LEVEL: WARN
      ENABLE_SWAGGER: 'false'
      ENABLE_DEBUG: 'false'
      JWT_SECRET: ${JWT_SECRET}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DOCKER_HOST: ${DOCKER_HOST}
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      SMTP_FROM_NAME: ${SMTP_FROM_NAME}
      ROOT_DOMAIN: ${ROOT_DOMAIN}
      FRONTEND_URL: https://${ROOT_DOMAIN}
      API_URL: https://api.${ROOT_DOMAIN}
      API_PREFIX: /api/v1
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.backend.rule=Host(`api.${ROOT_DOMAIN}`) || (Host(`${ROOT_DOMAIN}`) && PathPrefix(`/api`))'
      - 'traefik.http.routers.backend.entrypoints=web,websecure'
      - 'traefik.http.routers.backend.tls.certresolver=letsencrypt'
      - 'traefik.http.services.backend.loadbalancer.server.port=3001'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - backend-logs:/var/log/freebase
    networks:
      - freebase-core
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 500M
        reservations:
          cpus: '0.5'
          memory: 256M
    healthcheck:
      test: ['CMD', 'node', '-e', "require('http').get('http://localhost:3001/health', (r) => {if (r.statusCode !== 200) process.exit(1)}).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    image: ghcr.io/marion909/freebase/frontend:latest
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: https://api.${ROOT_DOMAIN}
      NEXT_PUBLIC_ROOT_DOMAIN: ${ROOT_DOMAIN}
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.frontend.rule=Host(`${ROOT_DOMAIN}`) && !PathPrefix(`/api`)'
      - 'traefik.http.routers.frontend.entrypoints=web,websecure'
      - 'traefik.http.routers.frontend.tls.certresolver=letsencrypt'
      - 'traefik.http.services.frontend.loadbalancer.server.port=3000'
    networks:
      - freebase-core
    restart: unless-stopped
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 500M
        reservations:
          cpus: '0.5'
          memory: 256M
    healthcheck:
      test: ['CMD', 'node', '-e', "require('http').get('http://localhost:3000', (r) => {if (r.statusCode !== 200) process.exit(1)}).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  freebase-core:
    driver: bridge

volumes:
  postgres-core:
  traefik-certs:
  backend-logs:
